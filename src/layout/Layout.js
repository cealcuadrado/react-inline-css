import './Layout.css';

function Layout() {
    return (
        <div className="layout">
            <h3 style={{color:'red'}}>This is the layout</h3>
            <p style={{'text-transform':'uppercase',color:'blue'}}>Hola, mundo!</p>
        </div>
    );
}
export default Layout;